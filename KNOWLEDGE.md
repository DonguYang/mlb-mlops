# 프로젝트 학습 중 정리한 개념들

> 코드 분석 과정에서 질문하고 정리한 개념 모음.

---

## 도구 / 인프라

### Git
코드 변경 이력을 추적하는 버전 관리 도구. 로컬 파일을 수정한다고 자동 반영되지 않음. `add → commit → push` 3단계를 직접 실행해야 GitHub에 올라감.

### GitHub Actions
GitHub가 자체 클라우드 서버에서 자동으로 작업을 실행해주는 서비스. 내 컴퓨터가 꺼져 있어도 동작. `push`, `schedule(cron)`, `workflow_dispatch(수동)` 등 트리거 조건을 yml 파일로 정의. 무료 플랜 기준 월 2,000분 사용 가능. 실행 환경은 일회성이라 작업이 끝나면 데이터와 모델이 사라짐 → Artifact 업로드로 보존 가능.

**개인 프로젝트에 권장되는 이유**: 별도 서버 불필요, yml 파일 하나로 설정, 유지보수 거의 없음, 무료.

### 의존성 (Dependency)
라이브러리를 설치할 때 함께 설치되는 다른 라이브러리들. `pandas`는 `numpy`만 필요해서 가볍지만, `apache-airflow`는 웹서버·스케줄러·DB 컴포넌트 등 수십 개를 함께 설치해서 무거움. 설치 시간이 길고 다른 라이브러리와 버전 충돌 위험도 높음.

### Airflow
복잡한 데이터 파이프라인을 관리하는 도구. 웹 UI + 스케줄러 + DB + 워커로 구성된 독립 시스템.

**권장 상황**:
- 파이프라인이 수십 개 이상이거나 작업 간 복잡한 의존 관계가 있을 때
- 여러 팀이 파이프라인을 공유하고 모니터링이 필요할 때
- 매일 수백 GB 이상 처리하거나 실패 시 비즈니스 영향이 클 때

이 프로젝트처럼 `수집 → 피처 → 학습` 3단계의 단순 파이프라인에는 과한 도구. GitHub Actions로 충분.

### Docker
실행 환경을 패키징하는 도구. Git과 별개로 함께 사용 가능.
- `Dockerfile`: 환경 설계도 (어떤 Python 버전, 어떤 라이브러리)
- 이미지: 설계도를 빌드한 완성본
- 컨테이너: 이미지를 실제로 실행한 인스턴스

```
Dockerfile → (build) → 이미지 → (run) → 컨테이너(실행 중인 서버)
```

### Parquet
데이터를 저장하는 파일 형식. CSV보다 빠르고 용량이 작음. pandas와 궁합이 좋아 `pd.read_parquet()` 한 줄로 DataFrame으로 바로 로드 가능. 이 프로젝트에서는 `.gitignore`에 등록되어 있어 Git에는 올라가지 않고 로컬에만 존재.

### MLflow
머신러닝 실험을 추적하는 도구.
- 어떤 파라미터로 학습했는지
- 성능 지표(accuracy, AUC)가 얼마였는지
- 학습된 모델 파일
를 자동으로 기록하고 버전 관리. `Production` 스테이지로 승격된 모델이 서빙에 사용됨.

---

## 웹/API 개념

### HTTP
인터넷에서 데이터를 주고받는 규칙(프로토콜). 요청(Request)을 보내면 응답(Response)을 돌려받는 구조.
- `GET`: 데이터를 달라고 요청 (조회)
- `POST`: 데이터를 보내면서 처리를 요청 (예측값 계산 등)

### API
프로그램끼리 대화하는 창구. 사람이 브라우저로 웹사이트에 접속하듯, 프로그램은 API로 다른 프로그램과 통신함. MLB 공식 API는 경기 데이터를 HTTP로 제공하고, 이 프로젝트의 api.py는 예측 결과를 HTTP로 제공.

### FastAPI
Python으로 HTTP API 서버를 쉽게 만들어주는 라이브러리. `@app.get("/health")`처럼 데코레이터로 엔드포인트를 정의.

### 엔드포인트
서버의 "창구 메뉴". URL별로 다른 동작을 함.
```
GET  /health      → 서버 생존 확인
GET  /model-info  → 현재 모델 버전 확인
POST /predict     → 예측 결과 반환
```

### Serving (서빙)
학습된 모델을 외부에서 사용할 수 있게 제공하는 것. api.py가 이 역할. 서버를 켜두면 Streamlit, 모바일 앱 등 어디서든 HTTP 요청으로 예측을 받을 수 있음. 개인 프로젝트에서는 Streamlit이 모델을 직접 불러와서 api.py를 생략하는 단순한 구조도 가능.

---

## Python 개념

### import
다른 파일이나 라이브러리의 코드를 가져와서 사용. 파일 상단에 모아두는 것이 관례.

### 전역변수 vs 지역변수
```python
x = 10  # 전역변수: 파일 어디서든 접근 가능

def 함수():
    y = 20      # 지역변수: 함수 안에서만 존재, 끝나면 사라짐
    global x    # 이 선언이 있어야 함수 안에서 전역변수를 수정 가능
    x = 99      # 전역변수 x가 바뀜
```

### 캐싱
한 번 만든 것을 저장해두고 재사용하는 패턴. 이 프로젝트에서는 모델 로딩(느림)을 처음 한 번만 하고 전역변수에 저장해두어, 이후 예측 요청에서는 저장된 모델을 바로 사용.

### Pipeline (sklearn)
전처리 + 모델을 하나로 묶는 것. 학습할 때와 예측할 때 동일한 전처리가 자동으로 적용됨.
```python
Pipeline([
    ("scaler", StandardScaler()),   # 1단계: 수치 정규화
    ("clf", LogisticRegression()),  # 2단계: 분류 모델
])
```

### iterrows
DataFrame을 한 행씩 꺼내서 반복하는 방법.
```python
for _, row in df.iterrows():
    print(row["home_team"])  # 경기 하나씩 순서대로 접근
```
`_`는 행 번호인데 쓰지 않아서 무시한다는 표시.

### deque (collections)
최대 크기가 정해진 큐. `deque(maxlen=10)`이면 11번째 항목이 들어올 때 첫 번째가 자동 제거. 이 프로젝트에서 "최근 10경기"를 유지하는 데 사용.

---

## 머신러닝 개념

### 피처(Feature)
모델에 입력되는 값들. 이 프로젝트의 피처 7개:
- `home_win_rate_l10`: 홈팀 최근 10경기 승률
- `away_win_rate_l10`: 원정팀 최근 10경기 승률
- `home_era` / `away_era`: 시즌 평균자책점
- `home_ops` / `away_ops`: 시즌 타격 종합지표
- `home_advantage`: 항상 1.0 (현재 무의미)

### 레이블(Label)
모델이 맞춰야 하는 정답. 이 프로젝트에서는 `home_win` (0 또는 1).

### 홀드아웃(Holdout)
모델 검증용으로 학습에 쓰지 않고 따로 빼두는 데이터. 이 프로젝트에서는 2024시즌. 모델이 한 번도 본 적 없는 데이터로 성능을 측정해야 실제 현실에서의 성능을 신뢰할 수 있음.

### 리키지(Data Leakage)
미래 정보가 학습 데이터에 새는 것. 예: 오늘 경기 결과를 포함해서 오늘 경기 승률을 계산하면 미래를 미리 아는 셈. 실험실 성능은 좋아 보이지만 실제로는 쓸 수 없는 모델이 됨. 이 프로젝트에서는 경기 결과를 기록하기 전에 승률을 먼저 저장하는 방식으로 방지.

### rolling 승률 (window)
직전 N경기 승률. `window=10`이면 바로 직전 10경기 결과의 평균. 시즌 전체 승률보다 "최근 상태"를 더 잘 반영함.

### StandardScaler
피처들의 스케일을 통일하는 전처리. ERA는 3~5, OPS는 0.6~0.9처럼 범위가 다른 값들을 평균 0, 표준편차 1로 변환. 로지스틱 회귀처럼 스케일에 민감한 모델에 필요. XGBoost 같은 트리 기반 모델은 불필요.

### Accuracy vs AUC
- **Accuracy**: 단순 정답률. "예측이 몇 % 맞았나"
- **AUC**: 0.5(동전 던지기)~1.0(완벽). 확률 예측의 질을 측정. 더 신뢰할 수 있는 지표.

### LogisticRegression vs XGBoost
| | LogisticRegression | XGBoost |
|--|--|--|
| 구조 | 선형 모델 | 결정 트리 앙상블 |
| 스케일 | StandardScaler 필요 | 불필요 |
| 해석 | 쉬움 | 어려움 |
| 성능 | 단순 패턴에 적합 | 복잡한 패턴에 강함 |

### Production 스테이지
MLflow에서 모델 버전에 붙이는 태그. 여러 버전 중 실제 서비스에 사용할 버전을 표시. `promote_best_model()`이 accuracy 기준으로 가장 좋은 모델을 자동 승격.

---

## 데이터 흐름 요약

```
MLB 공식 API
    ↓ collect.py
data/raw/
  schedules.parquet   (경기 결과: 날짜, 팀, 점수, 승패)
  team_stats.parquet  (팀 통계: OPS, ERA 등)
    ↓ features.py
data/processed/
  features.parquet    (ML 피처 7개 + 레이블)
    ↓ train.py
MLflow
  mlb_win_predictor   (학습된 모델, Production 승격)
    ↓ api.py (또는 Streamlit 직접 호출)
예측 결과             (홈팀 승리 확률)
```
